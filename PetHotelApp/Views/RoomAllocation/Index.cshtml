@model IEnumerable<PetHotelApp.Models.RoomAllocationModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Allocations</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AnimalName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoomType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CheckInDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CheckOutDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>            
                @Html.HiddenFor(modelItem => item.IdRoom)            
                @Html.HiddenFor(modelItem => item.IdAnimal)
            <td>
                @Html.DisplayFor(modelItem => item.AnimalName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoomType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CheckInDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CheckOutDate)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IdAllocation }) |
                @Html.ActionLink("Details", "Details", new { id = item.IdAllocation }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IdAllocation })
            </td>
        </tr>
}
    </tbody>
</table>
